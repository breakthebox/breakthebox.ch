# CLAUDE.md

## Project Overview

Break the Box ist eine webbasierte Applikation fuer [ZWECK].
Gebaut mit SvelteKit, TypeScript und Tailwind CSS.

## Governance Reference

- Verbindliche Projektprinzipien stehen in `.specify/memory/constitution.md`.
- Bei Widerspruechen gelten die Regeln in der Constitution vor lokalen Gewohnheiten.

## Tech Stack

<!-- ANPASSEN: Projektspezifische Details ergaenzen -->
- **Framework:** SvelteKit 2 / Svelte 5 / Vite 7
- **Language:** TypeScript (strict mode)
- **Database:** PostgreSQL 16 with Drizzle ORM
- **Auth:** Lucia v3 with Auth0 OAuth (via Arctic)
- **Styling:** Tailwind CSS 4 (Custom Design System)
- **AI:** Anthropic Claude API (`@anthropic-ai/sdk`)
- **Forms:** sveltekit-superforms + Zod 4
- **Testing:** Vitest + @testing-library/svelte + jsdom
- **i18n:** Paraglide JS 2.x (`@inlang/paraglide-js`) — de, en, fr
- **Containerization:** Docker + docker-compose

## Common Commands

```bash
# Development
docker compose up -d db    # Start PostgreSQL (first time)
npm run dev                # Start dev server (port 5173)
npm run build              # Production build

# Type checking
npm run check              # TypeScript + Svelte validation
npm run check:watch        # Watch mode

# Testing
npm run test               # Run Vitest in watch mode
npm run test:run           # Run tests once
npm run test:coverage      # Run tests with coverage

# Database (IMMER diesen Workflow einhalten!)
npm run db:generate        # Generate Drizzle migration from schema.ts changes
npm run db:migrate         # Apply pending migrations
npm run db:studio          # Launch Drizzle Studio UI
# ACHTUNG: npm run db:push NICHT verwenden — umgeht Migration-Tracking!

# Docker
docker compose up -d db    # Start only PostgreSQL
docker compose --profile full up -d  # Start DB + App
docker compose down        # Stop all services
```

## Project Structure

```
src/
  hooks.server.ts            # Auth middleware + CSRF + security headers
  routes/
    (app)/                   # Protected routes (require auth via hooks)
      dashboard/             # Dashboard
      settings/              # User settings
    (public)/                # Public routes
    auth/                    # Login, logout, OAuth callbacks
      login/                 # Login page
      logout/                # Logout endpoint
      callback/auth0/        # Auth0 OAuth callback
    api/                     # API endpoints
      ai/                   # AI endpoints
  lib/
    paraglide/               # Auto-generated by Paraglide (DO NOT EDIT)
      messages.js            # Compiled message functions
      runtime.js             # getLocale(), localizeHref(), etc.
      server.js              # paraglideMiddleware()
    components/              # Shared Svelte 5 components
      ui/                    # Base UI components (EmptyState, Toast, etc.)
    server/
      auth/                  # Auth module
        lucia.ts             # Lucia v3 setup + Drizzle adapter
        providers.ts         # OAuth providers (Auth0, optional Google)
        guards.ts            # requireAuth(), requireRole()
        oauth-callback.ts    # Shared OAuth callback handler
      db/                    # Drizzle ORM
        schema.ts            # Database schema (SINGLE SOURCE OF TRUTH)
        index.ts             # DB connection
        queries/             # Query functions by domain
      ai/                    # AI integration (Claude dispatch + mocks)
      validation/            # Zod schemas
    types/                   # Shared TypeScript types
    utils/                   # Utility functions
    config/                  # App configuration
    test/                    # Test infrastructure
      setup.ts               # Vitest global setup
      mocks/                 # Test mocks
      factories/             # Test data factories
messages/                    # Translation JSON files (de.json, en.json, fr.json)
project.inlang/             # Paraglide config (settings.json)
drizzle/                     # Migration SQL files (auto-generated!)
docs/                        # Design system + project documentation
```

## Key Architecture Rules

### Docker + PostgreSQL

Die Entwicklungsumgebung nutzt Docker Compose fuer PostgreSQL:

```bash
# Erster Start
cp .env.example .env         # Environment konfigurieren
docker compose up -d db      # PostgreSQL starten
npm run db:generate          # Erste Migration generieren
npm run db:migrate           # Schema anlegen
npm run dev                  # App starten
```

- **PostgreSQL** laeuft in einem Docker-Container (Port 5432)
- **Die App** laeuft lokal mit `npm run dev` (nicht im Container)
- Fuer Production-Tests: `docker compose --profile full up -d`
- DB-Daten persistieren im Docker Volume `pgdata`

### Authentication (Lucia v3 + Auth0)

- **Lucia v3** verwaltet Sessions mit Drizzle PostgreSQL Adapter
- **Auth0** als primaerer OAuth Provider (via Arctic Library)
- **Optional:** Google OAuth als zusaetzlicher Provider
- Session-Cookie: `secure` in Production, nicht in Dev
- CSRF-Schutz: Origin-Check auf non-GET Requests in `hooks.server.ts`

**Dateien:**
- `src/lib/server/auth/lucia.ts` — Lucia Setup + Type-Augmentation
- `src/lib/server/auth/providers.ts` — OAuth Provider Konfiguration
- `src/lib/server/auth/guards.ts` — `requireAuth()`, `requireRole()`
- `src/lib/server/auth/oauth-callback.ts` — Shared OAuth Callback Handler
- `src/hooks.server.ts` — Auth Middleware (Session-Validation, Route-Protection)

**Auth Flow:**
1. User klickt "Anmelden" → Redirect zu Auth0
2. Auth0 authentifiziert → Callback an `/auth/callback/auth0`
3. `oauth-callback.ts` erstellt/aktualisiert User + OAuth-Account
4. Lucia erstellt Session → Session-Cookie gesetzt
5. Alle `/(app)` Routen pruefen Session in `hooks.server.ts`

### Internationalisierung (Paraglide JS 2.x)

- **Paraglide JS 2.x** (`@inlang/paraglide-js`) fuer compile-time i18n
- Sprachen: Deutsch (Basis), Englisch, Franzoesisch
- Message-Dateien: `messages/{de,en,fr}.json`
- Kompilierte Ausgabe: `src/lib/paraglide/` (auto-generiert, NICHT editieren!)

**Verwendung in Komponenten:**
```svelte
<script>
  import * as m from '$lib/paraglide/messages.js';
</script>

<h1>{m.nav_dashboard()}</h1>
<p>{m.error_generic()}</p>
```

**Links lokalisieren (PFLICHT in Paraglide 2.x):**
```svelte
<script>
  import { localizeHref } from '$lib/paraglide/runtime';
</script>

<a href={localizeHref('/dashboard')}>Dashboard</a>
```

**Sprache wechseln:**
```svelte
<a data-sveltekit-reload href={localizeHref($page.url.pathname, { locale: 'en' })}>
  English
</a>
```

**WICHTIG:** `data-sveltekit-reload` ist bei Sprachwechsel-Links PFLICHT.

### Database Migrations (KRITISCH!)

Schema-Aenderungen MUESSEN ausschliesslich ueber den Drizzle-Migrations-Workflow erfolgen:

1. **Schema aendern** in `src/lib/server/db/schema.ts`
2. **Migration generieren** mit `npm run db:generate`
3. **Migration anwenden** mit `npm run db:migrate`

**Verbotene Praktiken:**
- **NIEMALS** SQL-Dateien manuell in `drizzle/` erstellen — ohne Journal-Eintrag werden sie ignoriert
- **NIEMALS** `db:push` verwenden — umgeht Migration-Tracking
- **NIEMALS** DDL direkt auf der DB ausfuehren ohne Drizzle-Workflow

### AI Integration
- AI-Dispatch mit Fallback-Kette: Claude API -> Mock-Daten
- Alle AI-Outputs gegen Zod-Schemas validieren
- Mock-Daten fuer jeden AI-Endpunkt bereitstellen (Offline-Entwicklung)

### Module Boundaries
- Components (`src/lib/components/`) duerfen NICHT von Server-Modulen importieren
- Shared Types leben in `src/lib/types/`
- Server-only Code lebt in `src/lib/server/`

### Svelte 5 Patterns
- **IMMER** `$props()`, `$state()`, `$derived()`, `$effect()` verwenden — keine Stores
- **IMMER** TypeScript `lang="ts"` in Script-Tags
- Komponenten exportieren Props via `$props()` mit Typen

## Schweizer Rechtschreibung

**IMMER** Schweizer Rechtschreibung verwenden: kein ß, immer ss. Gilt fuer:
- AI-Prompts und -Anweisungen
- UI-Texte und i18n-Strings (`messages/*.json`)
- Kommentare und Dokumentation auf Deutsch
- Beispiele: "Massnahme" (nicht "Maßnahme"), "gross" (nicht "groß"), "weiss" (nicht "weiß")

## Design System

<!-- ANPASSEN: Verweis auf Design-System-Dokumentation -->
**Vollstaendige Guidelines:** `docs/DESIGN_SYSTEM.md`

### Kritische Regeln

#### Humanisierung (KRITISCH!)
- **NIEMALS** technische Bezeichner im UI anzeigen
- Keine camelCase, snake_case, Template-IDs, Enum-Werte, UUIDs in der UI
- **IMMER** `m.message_key()` aus Paraglide fuer alle benutzersichtbaren Texte

#### DRY CSS Regeln
- **IMMER** Design-Token-Variablen statt hardcoded Werte
- **IMMER** Tailwind-Klassen mit CSS-Variablen (`bg-[var(--color-primary)]`)
- **NIEMALS** CSS-Patterns in mehr als 2 Dateien duplizieren

#### Error Handling (KRITISCH!)
- **NIEMALS** leere `catch`-Bloecke oder nur `console.error`
- **JEDER** `catch`-Block bei User-Facing-Code MUSS sichtbares Feedback geben (Toast/Notification)

#### Z-Index System
- **IMMER** CSS-Variablen verwenden — **NIEMALS** hardcodierte z-index Zahlen
- Kanonische Skala: `--z-dropdown: 100` → `--z-sidebar: 200` → `--z-sticky: 400` → `--z-overlay: 500` → `--z-modal: 1000` → `--z-toast: 2000`

#### Empty States
- **IMMER** shared `EmptyState`-Komponente verwenden
- **NIEMALS** inline HTML mit custom classes fuer leere Zustaende

#### Button Hierarchy
- Primary: filled/prominent
- Secondary/Cancel: ghost/outlined — **NIEMALS** identisch wie Primary
- Destructive: nur bei Hover sichtbar, Warn-Farbe

## Environment Variables

See `.env.example` for required configuration. Key variables:

- `DATABASE_URL` — PostgreSQL connection string
- `DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_PORT` — Docker Compose PostgreSQL config
- `AUTH0_DOMAIN` / `AUTH0_CLIENT_ID` / `AUTH0_CLIENT_SECRET` — Auth0 OAuth
- `ANTHROPIC_API_KEY` — Claude API key
- `PUBLIC_APP_URL` — Application URL (default: `http://localhost:5173`)
- `UPLOAD_DIR` — File storage directory (default: `./uploads`)
